const lists = []; // список задач. Брать из датасета

const showAll = () => {
    // показать список всех списков задач
}

const openList = (listId) => {
    // отрисовать список в контейнере
}

const closeList = () => {
    // закрыть список и показать все списки
}

const changeState = (taskId, state) => {
    // отправить запрос на изменение состояния задачи
}

const addTask = (listId, taskData) => {
    // добавить задачу в список
}